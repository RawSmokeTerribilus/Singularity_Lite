# RawLoadrr standalone requirements.
#
# Fully pinned to the versions running in the production image. See the header of
# the suite-level ../requirements.txt for why. Keep the two files in agreement:
# the Docker build installs the master list, so a drift here only shows up for
# someone running RawLoadrr outside the container.
aiofiles==25.1.0
aiohttp==3.14.1
anitopy==2.1.1
beautifulsoup4==4.15.0
# Ships BOTH the `bencode` and `bencodepy` top-level packages, which is why
# upload.py can `import bencodepy` and clients.py `import bencode` from a single
# distribution. Never add the standalone `bencodepy` project alongside it.
bencode.py==4.0.0
# cinemagoer 2026.06.27 removed the HTTP access system entirely and switched the
# default accessSystem from 'http' to 's3'. Cinemagoer() then builds an SQLAlchemy
# engine from the bogus default URI 'sqlite://cinemagoer.db', which SQLAlchemy 2.x
# rejects, so every construction raises ArgumentError before any lookup happens.
cinemagoer==2023.5.1
deluge-client==1.10.2
ffmpeg-python==0.2.0
guessit==3.8.0
Jinja2==3.1.6
language-data<=1.2.0; python_version<='3.8'
langcodes<=3.4.0; python_version<='3.8'
langcodes==3.5.1; python_version>'3.8'
nest_asyncio==1.6.0
packaging==26.2
pyimgbox==1.0.7
pyotp==2.9.0
pyoxipng==9.1.1
pymediainfo==7.0.1
# Sólo se usa para LISTAR lo que hay dentro de un .rar, nunca para extraer:
# las cabeceras RAR3/RAR5 las parsea en Python puro, así que no arrastra
# extensiones en C ni necesita el binario `unrar`, que en Debian es non-free.
# Sin él, un .rar simplemente no se puede abrir y se le pregunta al operador.
rarfile==4.2
qbittorrent-api<=2023.9.53; python_version<'3.8'
qbittorrent-api==2026.6.0; python_version>='3.8'
requests[socks]==2.34.2
PySocks==1.7.1
rich==15.0.0
tinytag==2.2.1
torf==4.3.1
tmdbsimple==2.9.6
unidecode==1.4.0
